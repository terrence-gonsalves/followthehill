// Step 7: POST /api/stripe/support — one-time support payment
export async function POST(request: Request) {
  return Response.json({ message: "Stripe support — coming in Step 7" }, { status: 501 })
}
