// Step 6: POST /api/ai/donor-insight — AI donor pattern insight (requires subscription)
export async function POST(request: Request) {
  return Response.json({ message: "AI donor-insight — coming in Step 6" }, { status: 501 })
}
